// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicSectionMobileMenuLink,
  DefaultSectionMobileMenuLinkProps
} from "./plasmic/copy_of_sips_by_website/PlasmicSectionMobileMenuLink";
import { HTMLElementRefOf } from "@plasmicapp/react-web";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface SectionMobileMenuLinkProps extends Omit<DefaultSectionMobileMenuLinkProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultSectionMobileMenuLinkProps altogether and have
// total control over the props for your component.
export interface SectionMobileMenuLinkProps
  extends DefaultSectionMobileMenuLinkProps {}

function SectionMobileMenuLink_(
  props: SectionMobileMenuLinkProps,
  ref: HTMLElementRefOf<"a">
) {
  // Use PlasmicSectionMobileMenuLink to render this component as it was
  // designed in Plasmic, by activating the appropriate variants,
  // attaching the appropriate event handlers, etc.  You
  // can also install whatever React hooks you need here to manage state or
  // fetch data.
  //
  // Props you can pass into PlasmicSectionMobileMenuLink are:
  // 1. Variants you want to activate,
  // 2. Contents for slots you want to fill,
  // 3. Overrides for any named node in the component to attach behavior and data,
  // 4. Props to set on the root node.
  //
  // By default, we are just piping all SectionMobileMenuLinkProps here, but feel free
  // to do whatever works for you.

  return <PlasmicSectionMobileMenuLink root={{ ref }} {...props} />;
}

const SectionMobileMenuLink = React.forwardRef(SectionMobileMenuLink_);
export default SectionMobileMenuLink;
